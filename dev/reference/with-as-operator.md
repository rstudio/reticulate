# Create local alias for objects in `with` statements.

Create local alias for objects in `with` statements.

## Usage

``` r
object %as% name
```

## Arguments

- object:

  Object to alias

- name:

  Alias name
