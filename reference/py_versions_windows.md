# Discover versions of Python installed on a Windows system

Discover versions of Python installed on a Windows system

## Usage

``` r
py_versions_windows()
```

## Value

Data frame with `type`, `hive`, `install_path`, `executable_path`, and
`version`.
