# Provide help for Python objects

This is an internal method to be used by front-ends which need to
provide help text / information for Python objects in different
contexts.

## Usage

``` r
py_help_handler(type = c("completion", "parameter", "url"), topic, source, ...)
```
