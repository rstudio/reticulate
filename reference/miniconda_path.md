# Path to Miniconda

The path to the Miniconda installation to use. By default, an
OS-specific path is used. If you'd like to instead set your own path,
you can set the `RETICULATE_MINICONDA_PATH` environment variable.

## Usage

``` r
miniconda_path()
```
